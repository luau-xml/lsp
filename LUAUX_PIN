# The compiler revision this server is built and tested against.
#
# Pin, never float. A language server built against a different compiler version
# than the one producing `build/` reports diagnostics the build does not, which
# is worse than reporting none.
#
# CI clones this revision into `../luaux`, which is where the workspace's path
# dependency points — so local development and CI compile the same two crates
# against each other.

repository = https://github.com/luau-xml/luaux
rev = 5c4dc7eef33940ae2f6541c00bb21341206eab8b
version = 0.1.3-canary.2
